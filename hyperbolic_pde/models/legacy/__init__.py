"""Legacy / superseded LWR model lineages.

These modules are retained for reference and reproducing older results but are
no longer the canonical models (HypNO-ST3 is). They are not imported by
``hyperbolic_pde.models`` by default; import them explicitly from
``hyperbolic_pde.models.legacy`` if needed.
"""
